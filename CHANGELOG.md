# 更新日志

## v2.2.0 - 2026-09-18

本次发布含两部分整改：界面与交互整改（含少量新功能）与稳定性/工程化整改；
均不改变数据采集口径。

### UI/UX 整改

#### 新功能

- **独立窗口记住上次位置**：图表/流量统计/TCP 三个窗口关闭后再次打开回到上次位置
  （写入 `~/.config/jnetapplet/settings.ini` 的 `geometry/<窗口>`，多显示器下若原位置
  已不存在则回退居中）
- **弹窗详情入口**：弹窗内新增"流量波动图 / 流量统计 / TCP 连接"三个文字入口，
  不再只有右键菜单一条发现路径
- **弹窗支持 Esc 关闭**（是否生效取决于面板是否把键盘焦点交给弹窗，属尽力而为）
- **三个独立窗口均支持置顶**：图表/流量统计/TCP 窗口标题栏都有置顶按钮，
  置顶状态分别持久化，重启后保持

#### 修复

- **流量统计"今日/本月累计"名不符实**：顶部改显示真实的本期合计（按日=今日、按月=本月，
  无记录显示"—"）；底部汇总文案改为"全部记录合计"，两种口径不再混用
- **弹窗包统计缺少时间基准**：`Packets` 改为 `Packets (since boot)`，
  避免与会话累计流量并列时被理解为同一口径
- **图表时间文案硬编码英文**：1/5/30 分钟按钮与 X 轴刻度（`now`、`-5 min`）全部走翻译
- **弹窗关闭后悬停提示丢失**：关闭弹窗且鼠标仍在图标上时自动恢复提示
- **卸载提示被裁切**：76 字符提示不再塞进 40px 高的按钮，改为按钮下方独立状态行
- **按钮文字锚点冲突**：`anchors.centerIn` 与 `anchors.left/right` 同时声明导致
  运行期锚点告警（行为不确定），改为垂直居中 + 左右边距
- **任务栏整数位宽度估算偏窄**：改用 `TextMetrics` 按实际字号实测，
  不再用 `dockSize × 0.14` 的经验比例
- **弹窗内容被裁切**：`PanelPopup` 尺寸原为硬编码 `330x348`，中文文案下内容实测需 `412x377`
  （包统计行宽 388px，底部还有"详情入口"行），导致右侧（上传速度单位、错误/丢包、
  第三个接口 chip）与底部三个详情入口被窗口边界裁掉；改为
  `Math.max(330, 内容隐式宽)` / `Math.max(348, 内容隐式高)`，由内容实测尺寸驱动，
  翻译文案、字号、数值长度变化都不再裁切（下限保留原值，避免占位态弹窗塌缩）
- **流量波动图时间窗口按钮文字重叠**：按钮宽度固定 `24px`（按英文 `1m/5m/30m` 设计），
  文案改走翻译后中文"1 分钟 / 5 分钟 / 30 分钟"实测宽 28~33px，
  文字居中溢出并与相邻按钮、右侧接口标签压叠；改为按各按钮文案实测宽度 + 12px 内边距

#### 优化

- 强调色收敛到 `NetCommon`（新增高亮蓝与危险色系），消除跨窗口色值漂移：
  关于窗口的链接色与图表置顶色原为两处各写的 `#0081FF`，复制成功色统一为
  `accentGreen`（`#22A34A` → `#16A34A`，肉眼几乎不可辨）
- **趋势图折线配色统一为"下载蓝、上传绿"**：原用绿色画下载、橙色画上传，与弹窗/任务栏
  （下载蓝、上传绿）语义相反；现折线/圆点/图例箭头统一取自 `NetCommon`，
  并为 Canvas 补充强调色的字符串形式（Canvas 无法解析 QML color 的序列化结果）
- 取色器删除自带的第四套深浅色板，改用 `WindowTheme`
- 可点元素补齐按下态（chip、tab、区间按钮、色块、关闭按钮等约 25 处）
- 表格列宽改为按窗口宽度比例推导（统计窗口 20%/18%/余下三等分，
  TCP 窗口 27%/27%/14%+剩余给进程名），并设最小宽度
- 首次为可交互元素补充无障碍标注（角色 + 名称，约 20 处）：
  接口 chip、置顶/关闭/清空搜索等纯图标按钮、tab 与色块

#### 重构

- **独立窗口外壳统一为 `WindowShell`**：无边框外观、圆角卡片、标题栏、可选置顶按钮、
  位置与置顶持久化收敛为一份实现（三个窗口合计减少 305 行）；
  图表窗口的置顶实现从图表绘制代码中移出
- 后端新增 `windowGeometry`/`saveWindowGeometry`/`windowPinned`/`setWindowPinned`，
  键名走白名单，与既有设置共用一份 `settings.ini`

#### 验证情况与已知限制

- 本轮改动已在真机编译、安装并渲染（两处尺寸缺陷正是由此暴露并修复）；
  受限于仓库环境（无 C++ 编译器、无 QML 运行时），本地只能做静态检查与离线布局探针，
  **未做屏幕阅读器实测**
- 弹窗 Esc 关闭依赖面板把键盘焦点交给 layer-shell 弹窗，很可能不生效，
  需真机确认后决定是否改由 dde-shell 侧处理
- 两处尺寸缺陷的定位与复测方法（探针为临时诊断产物，已清理）：
  用 PySide6 加载 `package/components` 下真实组件 + 真实中文 `.qm`，离屏渲染读取布局数值。
  修复前弹窗内容隐式 `412x377` vs 窗口 `330x348`、选择器按钮文案 28~33px vs 按钮 24px；
  修复后弹窗内所有子项均落在窗口内、按钮文案两侧各余 6px
- 弹窗尺寸绑定（`PanelPopup`）与三个独立窗口观感已真机确认（2026-09-18）：
  dde-shell 接受按内容隐式尺寸的运行时尺寸变化，弹窗右侧（上传速度单位、错误/丢包、
  第三个接口 chip）与底部三个详情入口不再被裁切，图表顶部时间窗口按钮不再叠字
- 趋势图折线的上传绿取自 `NetCommon.accentGreen`（#16A34A），比原先的亮绿更暗，
  深色背景下是否足够清晰需真机确认（若要调亮，只改 `NetCommon` 一处即可）

### 稳定性与工程化

本轮以稳定性、性能与工程化为主，无新功能。

#### 修复

- **流量统计日志原子写入**：改用 `QSaveFile` 临时文件 + 原子替换，避免写入中断导致
  JSON 损坏、30 天历史被整体清零；损坏降级由静默改为记录告警
- **子进程不再阻塞任务栏**：`ss`/`iw` 改为异步执行（原同步等待最长 3 秒会冻结与
  applet 同进程的 dde-shell 面板），保留超时上限与"不可用即降级"的原有行为
- **趋势图越界绘制**：折线/填充按可见时间窗裁剪并加 `ctx.clip()`，不再压住 Y 轴刻度
  与顶部信息条
- **Y 轴自适应窗口**：改为按时间戳取"最近 60 秒"，此前按 60 个采样点计算
  （刷新间隔 5 秒时实际为 300 秒）
- **TCP 窗口搜索框清空失效**：`text` 绑定被输入框内部赋值销毁，点击 × 后输入框仍显示
  旧关键字
- **表格列错位**：表头/数据行/汇总行三套左边距统一，汇总行补齐两列间距
- **设置窗口高度锁死**：`minimumHeight == maximumHeight == 450` 在多网卡时可能裁掉
  底部卸载区域
- **卸载命令与提示态**：命令改为单一来源，避免"显示的命令"与"复制的命令"不一致；
  提示态改用独立布尔量，不再由界面文案反推
- **版本号兜底**：移除 QML 中硬编码的 `"1.0"`，统一由后端 `PROJECT_VERSION` 兜底

#### 优化

- 正则提升为 `static const`，消除逐行/逐连接重复编译
- 图表平滑值预计算一次供填充与描边共用（原同一点计算两遍）
- 趋势图窗口不可见时不再重绘
- TCP 与流量统计窗口的刷新定时器对齐注释所述的 5 倍降频，隐藏时不再重建模型
- `build-deb.sh` 依赖预检补充 `dpkg`，并删除 `set -e` 下不可达的架构兜底分支

#### 重构

- 设置持久化收敛为 `configFilePath()` + `persistSetting()`（原四处重复拼接路径）
- 删除只写不读的 `m_currentDate`/`m_currentMonth` 与无人引用的 `sortedInterfaces`
- 接口排序统一到 `NetCommon.sortInterfaces()`，弹窗与设置窗口顺序一致
- 链路速率/TCP 清单解析抽出为独立函数（`readSysfsLinkSpeed`/`parseTcpConnectionList` 等）

#### 构建与测试

- 移除未使用的 `Qt6::DBus` 链接
- 新增 6 项纯逻辑单元测试（流量日志累加/裁剪/损坏降级、`iw` 与 `ss` 输出解析）
- 新增 GitHub Actions 静态检查（shellcheck，error 级别）
- README/AGENTS 与实现对齐（测试说明、依赖包、项目结构、功能清单）

## v2.1.0 - 2026-08-09

v2.0.0 后的优化版本。

### 优化

- 流量统计裁剪上限调整：按日保留 30 天，按月保留 12 个月

## v2.0.0 - 2026-08-09

本次为大版本更新，涵盖弹窗重新设计、多个新功能窗口、性能优化和架构重构。

### 新功能

- **左键弹窗重新设计**：通透分区布局（分隔线+留白），速度数字 24px 视觉锚点，适配 Deepin 原生毛玻璃风格
- **IPv4/IPv6 一键复制**：地址旁加复制图标，点击写入剪贴板，复制后显示 ✓ 反馈
- **日/月流量统计**（持久化）：右键菜单新增"流量统计"窗口，按日/按月展示累计流量，跨重启累加，JSON 持久化到磁盘
- **TCP 连接清单**：右键菜单新增"TCP 连接"窗口，展示所有 ESTABLISHED 连接的本地/远程地址端口、状态、进程名，支持搜索筛选
- **链路速率显示**：弹窗接口信息区显示协商速率（Gbps/Mbps）
- **WiFi 信号强度**：无线接口显示信号 dBm
- **包统计/错误/丢包**：弹窗累计流量区展示收发包数、错误数、丢包数
- **TCP 连接数**：弹窗显示整机活跃 TCP 连接数
- **可配置刷新间隔**：设置窗口新增 1/2/5 秒刷新间隔选项，影响全项目所有定时器
- **流量波动图时间窗口**：1/5/30 分钟可切换
- **任务栏数值小数点对齐**：双行网速数值小数点垂直对齐，箭头间距固定

### 优化

- 弹窗文字颜色改用 dock 上下文调色板，深色模式文字清晰可见
- 弹窗高度固定，切换网卡时不再跳动
- IPv4/IPv6 地址行固定宽度标签对齐，不再随地址长度错位
- 流量波动图曲线 5 点滑动平均，抑制亚秒混叠尖峰
- C++ 后端性能优化：正则预编译、信号脏标记、IP 检测降频、历史 QVariantList 缓存
- TCP 进程名反查改用 ss 命令（netlink），不受 /proc 权限限制

### 重构

- 提取 WindowTheme + TitleBar 组件，消除三个独立窗口约 150 行样板重复
- 提取 interfaceIcon/interfaceDescription 到 NetCommon，消除跨组件重复
- 清理 fontScale 死代码，移除 30+ 处乘法噪声
- CMake Test 改为可选依赖，解决无 Qt6 Test 开发包环境构建失败

### 其他

- 右键菜单顺序调整：流量波动图 -> 流量统计 -> TCP 连接 -> 设置 -> 关于
- 全项目国际化翻译补全（77 条翻译）
- 任务栏数值右侧留边距
